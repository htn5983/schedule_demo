[project]
name = "time-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "htn5983", email = "a0925072598@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "datetime>=6.0",
    "schedule>=1.2.2",
]

[project.scripts]
time-demo = "time_demo:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
